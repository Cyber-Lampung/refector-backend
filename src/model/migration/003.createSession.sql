CREATE TABLE sessions (session_id VARCHAR AUTO_INCREMENT NOT NULL PRIMARY KEY, user_id VARCHAR(100) NOT NULL, sessions VARCHAR(255) NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL, expires_at DATETIME NOT NULL)
